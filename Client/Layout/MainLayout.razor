@inherits LayoutComponentBase
@inject ClientModeService ModeService

<Offcanvas @ref="tasksList"/>

<div class="page">
    <div class="sidebar">
        <NavMenu />
    </div>

    <main>

        <div class="top-row px-4 button-right-container">

            @if (@ModeService.CurrentMode == ClientMode.Normal)
            {
                <button class="btn btn-danger" @onclick="()=>ModeService.ToggleMode()" > Report a bug </button>
                <button class="btn btn-info" @onclick="ShowTasksList"> Show tasks list </button>
            }
            else 
            {
                <button class="btn btn-info btn-forward" @onclick="()=>ModeService.ToggleMode()" > Do not report </button>
                <button class="btn btn-success"> Confirm </button>
            }
        </div>

        <article class="content px-4">
            @Body
        </article>
    </main>
</div>

@code
{
    private Offcanvas tasksList = default!;

    private async Task ShowTasksList()
    {
        var parameters = new Dictionary<string, object>();
        await tasksList.ShowAsync<TasksList>(title: "Tasks list:", parameters: parameters);
    }
}
